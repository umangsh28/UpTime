<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.uptimeapp" >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UpTimeApp"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,ExtraText">
        <activity
            android:name=".InHomeFragement.BookReader"
            android:exported="true" />
        <activity
            android:name=".PaymentActivity"
            android:exported="true"
            android:label="@string/title_activity_payment"
            android:theme="@style/Theme.UpTimeApp.NoActionBar" />
        <activity
            android:name=".DeepDiveActivity"
            android:exported="true" />
        <activity
            android:name=".BookDetailsActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity
            android:name=".SplashActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>